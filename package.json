{
  "name": "urlargs",
  "version": "1.0.0",
  "description": "Lightweight, type-safe utility for parsing URL query parameters.",
  "author": "George Michael Brower",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/urlargs.js",
      "types": "./dist/urlargs.d.ts",
      "source": "./urlargs.ts"
    }
  },
  "files": [
    "dist",
    "urlargs.ts",
    "README.md",
    "LICENSE.md"
  ],
  "scripts": {
    "build": "tsc urlargs.ts --target esnext --declaration --outDir ./dist",
    "check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "vite",
    "prepublishOnly": "npm run clean && npm run build && npm run test",
    "lint": "eslint . --fix",
    "test": "vitest run --environment jsdom"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/georgealways/urlargs.git"
  },
  "devDependencies": {
    "eslint": "^9.27.0",
    "eslint-config-gmb": "^0.11.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.2",
    "vite": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
